# Copy this into your existing .env (or merge these lines into it)

SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here   # Settings → API → service_role (SECRET, server-only)

SESSION_SECRET=change-this-to-a-long-random-string
